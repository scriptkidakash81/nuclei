name: Call Gosec Scan Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  call-gosec:
    uses: scriptkidakash81/ci-templates-gosec/.github/workflows/gosec.yml@main
    with:
      branch: main
      
